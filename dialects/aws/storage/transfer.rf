# Generated by scripts/generate-aws-dialect.ts.
concept "transfer-connector" {
  kind        = entity
  description = "AWS Transfer Family connector as a durable AWS architecture entity."
}

concept "transfer-server" {
  kind        = entity
  description = "AWS Transfer Family server as a durable AWS architecture entity."
}

concept "transfer-workflow" {
  kind        = entity
  description = "AWS Transfer Family workflow as a durable AWS architecture entity."
}

rule "transfer-connector" {
  match {
    kind = "resource"
    type = "aws_transfer_connector"
  }

  as = concept.transfer-connector
}

rule "transfer-server" {
  match {
    kind = "resource"
    type = "aws_transfer_server"
  }

  as = concept.transfer-server
}

rule "transfer-workflow" {
  match {
    kind = "resource"
    type = "aws_transfer_workflow"
  }

  as = concept.transfer-workflow
}
