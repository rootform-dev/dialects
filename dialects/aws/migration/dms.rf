# Generated by scripts/generate-aws-dialect.ts.
concept "dms-endpoint" {
  kind        = entity
  description = "AWS Database Migration Service (AWS DMS) endpoint as a durable AWS architecture entity."
}

concept "dms-replication-instance" {
  kind        = entity
  description = "AWS Database Migration Service (AWS DMS) replication instance as a durable AWS architecture entity."
}

concept "dms-replication-task" {
  kind        = entity
  description = "AWS Database Migration Service (AWS DMS) replication task as a durable AWS architecture entity."
}

concept "dms-s3-endpoint" {
  kind        = entity
  description = "AWS Database Migration Service (AWS DMS) S3 endpoint as a durable AWS architecture entity."
}

rule "dms-endpoint" {
  match {
    kind = "resource"
    type = "aws_dms_endpoint"
  }

  as = concept.dms-endpoint
}

rule "dms-replication-instance" {
  match {
    kind = "resource"
    type = "aws_dms_replication_instance"
  }

  as = concept.dms-replication-instance
}

rule "dms-replication-task" {
  match {
    kind = "resource"
    type = "aws_dms_replication_task"
  }

  as = concept.dms-replication-task
}

rule "dms-s3-endpoint" {
  match {
    kind = "resource"
    type = "aws_dms_s3_endpoint"
  }

  as = concept.dms-s3-endpoint
}
